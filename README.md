The Python file in this repository is used for decoding a stenographic image from wikipedia. It does this by taking only the two least significant bits in each color component and normalizing the resulting image. 